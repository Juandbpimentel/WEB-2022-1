import React from 'react';

const Enemyq2 = (props) => {

    return (
        <div>
            <img src={props.url} />
            <p>Nome: {props.name}</p>
        </div>
    )
}

export default Enemyq2;